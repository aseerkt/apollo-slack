type Team {
  id: Int!
  name: String!
  owner: User!
  members: [User!]!
  channels: [Channel!]!
}

type Query {
  allTeams: [Team!]!
  getTeam(teamId: Int!): Team
}

type Mutation {
  createTeam(name: String!): DefaultResponse!
}
